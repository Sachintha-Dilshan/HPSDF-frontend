name: CI [Manual] Product Info Integration

on:
  workflow_dispatch:

jobs:

  Build:
    uses: ./.github/workflows/build.yaml
    with:
      GITHUB_ENVIRONMENT: 'DEV'
    secrets: inherit
    needs:
      - Test